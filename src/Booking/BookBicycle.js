import React from 'react';
import { Outlet } from 'react-router-dom';

const BookBicycle = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default BookBicycle;